# cloud/

云端训练/推理相关脚本与镜像定义，与 Ultralytics 包本体逻辑无关。

## 文件清单

| 文件 | 说明 |
| --- | --- |
| `dlc/` | 阿里云 PAI-DLC 云端 YOLO 训练包（镜像 + 数据整形/上传/提交），详见 `dlc/CLAUDE.md` |

## 调用链

- 本地命令行运行 `dlc/` 下的工具（`prepare.py` 纯本地，无需云依赖）。
- 需要云凭据的步骤（push ACR、传 OSS、提交 DLC）由用户在控制台或本机 CLI 执行，本目录只提供脚本/文档。
